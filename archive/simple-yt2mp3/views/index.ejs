<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>YouTube to MP3 Converter</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: #fff;
            color: #6C3FCF;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        main {
            background: #fff;
            color: #111;
            border-radius: 20px;
            box-shadow: 0 8px 32px rgba(108,63,207,0.15);
            padding: 3.5rem 4rem;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-width: 400px;
            max-width: 600px;
        }
        h1 {
            margin-bottom: 0.5rem;
            text-align: center;
            font-size: 2rem;
            font-weight: 700;
        }
        p {
            margin-bottom: 1.5rem;
            text-align: center;
        }
        form {
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 100%;
        }
        label {
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: #6C3FCF;
        }
        input[type="url"] {
            padding: 0.75rem 1.2rem;
            border: none;
            border-radius: 10px;
            margin-bottom: 1rem;
            width: 100%;
            max-width: 320px;
            font-size: 1rem;
            color: #fff;
            background: linear-gradient(90deg, rgba(108,63,207,0.85) 0%, rgba(108,63,207,0.65) 100%);
            box-shadow: 0 2px 8px rgba(108,63,207,0.10);
            outline: none;
        }
        input[type="url"]::placeholder {
            color: #e0d6fa;
            opacity: 1;
        }
        button {
            background: linear-gradient(90deg, rgba(108,63,207,0.85) 0%, rgba(108,63,207,0.65) 100%);
            color: #fff;
            border: none;
            border-radius: 10px;
            padding: 0.75rem 2rem;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            box-shadow: 0 2px 8px rgba(108,63,207,0.15);
            transition: background 0.2s, box-shadow 0.2s;
        }
        button:hover {
            background: linear-gradient(90deg, rgba(83,38,163,0.95) 0%, rgba(108,63,207,0.85) 100%);
            box-shadow: 0 4px 16px rgba(108,63,207,0.20);
        }
        #result-container {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-top: 2rem;
        }
        .success {
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 4px 24px rgba(108,63,207,0.10);
            padding: 2rem 2.5rem;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-width: 320px;
            max-width: 400px;
        }
        .success h2 {
            color: #111;
            margin-bottom: 1rem;
            text-align: center;
        }
        .success p {
            color: #6C3FCF;
            margin-bottom: 1.5rem;
            text-align: center;
        }
        #download-btn {
            display: inline-block;
            background: linear-gradient(90deg, rgba(108,63,207,0.85) 0%, rgba(108,63,207,0.65) 100%);
            color: #fff;
            border: none;
            border-radius: 10px;
            padding: 0.75rem 2rem;
            font-size: 1rem;
            font-weight: 600;
            text-decoration: none;
            box-shadow: 0 2px 8px rgba(108,63,207,0.15);
            transition: background 0.2s, box-shadow 0.2s;
            text-align: center;
        }
        #download-btn:hover {
            background: linear-gradient(90deg, rgba(83,38,163,0.95) 0%, rgba(108,63,207,0.85) 100%);
            box-shadow: 0 4px 16px rgba(108,63,207,0.20);
        }
        .error {
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 4px 24px rgba(255,0,0,0.08);
            padding: 2rem 2.5rem;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-width: 320px;
            max-width: 400px;
        }
        .error h2 {
            color: #d32f2f;
            margin-bottom: 1rem;
            text-align: center;
        }
        .error p {
            color: #6C3FCF;
            margin-bottom: 1.5rem;
            text-align: center;
        }
    </style>
</head>
<body>
    <main>
        <div id="top-container">
            <h1>YouTube to MP3 Converter</h1>
            <p>Convert your favorite YouTube videos to MP3 audio easily and quickly.</p>
            <form action="/convert-mp3" method="POST" id="form">
                <label for="youtube-url">YouTube Video URL:</label>
                <input type="url" id="youtube-url" name="youtubeUrl" required placeholder="Paste YouTube link here">
                <button type="convert-btn">Convert</button>
            </form>
        </div>
        <div id="result-container">
            <% if (typeof success != "undefined" && success) { %>
                <div class="success">
                    <h2><%= song_title %></h2>
                    <p>Your MP3 file is ready for download:</p>
                    <a href="<%= song_link %>" id="download-btn" download>Download</a>
                </div>
            <% } else if (typeof success != "undefined" && !success) { %>
                <div class="error">
                    <h2>Error Has Occurred</h2>
                    <p><%= message %></p>
            <% } %>
                </div>
        </div>
    </main>
</body>
</html>